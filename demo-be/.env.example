# Environment
NODE_ENV=production

# Server Port
PORT=3000

# Database Configuration (will connect to Server 3)
# These will be updated after DB server is deployed
DB_HOST=10.xxx.xxx.xxx    # Database server PRIVATE IP (fill in later)
DB_PORT=5432
DB_NAME=todo_db
DB_USER=todouser
DB_PASSWORD=your_db_password_here    # Set strong password

# CORS - Allow frontend domain
ALLOWED_ORIGINS=https://fatu.io.vn,https://www.fatu.io.vn
